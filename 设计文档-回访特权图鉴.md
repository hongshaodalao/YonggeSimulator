# 勇哥模拟器 · 回访/特权/图鉴 设计文档

> 基础规则见前几份设计文档，本文档只写增量。三项内容：**老客户回访、头衔特权、案例图鉴**。存档升 v3。

---

## 1. 老客户回访

结案后，老客户会在后续连线间隙打回电话来。回访带选择分支，影响**粉丝**与新增的**江湖声望（rep，0~100，初始 50）**。

**数据结构**（案例级可选字段 `callback`）：

```js
callback: {
  after: 3,                       // 结案后再完成 N 单后触发
  intro: "...",                   // 回访开场白；可选用 introOk / introBad
  introOk: "...", introBad: "...",// 按"当时判对/判错"分支（提供则优先）
  choices: [
    { label: "...", fans: 20, rep: 5, reply: "..." },
  ],
}
```

**运行规则**
- 触发计数只在正常模式（故事/无尽）推进；每日挑战不推进、不触发。
- 结案时：所有待回访 `ticks--`；若本案有 `callback` 且从未回访过、也不在队列中 → 入队 `{caseId, ticks, ok}`（ok=当时是否判对，用于 introOk/introBad 分支）。
- `startCase` 时若有 `ticks<=0` 的回访，先弹回访覆盖层，选完再进入本案；同一时间只弹一单回访。
- 已回访过的案例（`firedCallbacks`）不再触发；粉丝变动与判决同规则（下限 0），声望夹在 0~100。
- 回访状态入存档，刷新后待回访的会重新弹起。

**首批 5 条回访**：小王(+3单)、小张(+4)、陈叔(+4)、小于(+5)、老赵(+6)。判对/判错走不同开场白，三个选择分别对应"高情商/务实/嘴硬"人格路线，粉丝与声望增减不同。

## 2. 头衔特权

经验头衔从"荣誉牌"变成有实际手感的被动技能，逐级累计：

| 头衔门槛 | 特权 |
|---|---|
| 连线常客（≥100） | 开局弹幕剧透：随机提示一个关键问题所属的话题方向 |
| 餐饮参谋（≥260） | 每单提问名额 +1（5→6） |
| 选址军师（≥450） | 每单首个 🎯 动作类问题**不占名额**（蹲点/问同行/查工商/要流水任选其一） |
| 勇哥本尊（≥650） | 提问名额再 +1（累计 5→7） |

- 特权在故事/无尽/每日挑战全部生效（挑战同规则，公平）。
- 名额上限实时按当前头衔计算；免费动作每单限一次，记 `cur.freeActionUsed`。

## 3. 案例图鉴

- 顶栏 📖 打开：**12 案例 × 原版/B 卷 = 24 格**收集墙，附头衔特权一览。
- 遭遇过的格子点亮并显示该卷正确判决；未遭遇显示"？？？未遭遇"。跨存档保留（新开档不清空）——图鉴记录的是"见识过什么"。
- 记录时机：判决结算时写入 `codex[caseId:卷号]`（每日挑战遭遇同样计入）。
- `instantiate` 产物携带 `vi`（卷号，原版 0），供图鉴与回访定位。

## 4. 存档 v3

- v:3 新增 `rep=50、pendingCallbacks=[]、firedCallbacks=[]、codex={}`；兼容 v1/v2 旧档（新字段取默认值）。
- 新开档：重置 rep/回访队列，**保留 codex**（图鉴是跨档收集）。

## 5. 验收清单

- [ ] 回访按 after 计数触发，introOk/introBad 分支正确，选择影响粉丝与声望并显示明细
- [ ] 挑战模式不推进回访；已回访不重复触发；刷新后待回访重新弹起
- [ ] 四档特权逐级生效：弹幕提示、+1 名额、免费动作、+2 名额
- [ ] 图鉴 24 格遭遇点亮、未遭遇 ？？？、跨新开档保留；特权一览显示当前生效状态
- [ ] v2 旧档迁移无感知
