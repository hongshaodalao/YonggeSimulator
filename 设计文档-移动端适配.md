# 勇哥模拟器 · 移动端适配 设计文档

> 现状：仅有 viewport 标签和 860px 单断点（双栏堆叠）。390×844 实测发现两处硬伤与若干细节问题，本文档给出修复规格。

---

## 1. 实测问题（390×844）

| # | 问题 | 严重度 | 根因 |
|---|---|---|---|
| 1 | 顶栏溢出挤爆：品牌名竖排、11 项内容塞一行互相挤压 | 高 | 顶栏单行 flex 无换行，窄屏最小内容宽超屏宽 |
| 2 | 全部弹窗右侧裁切（开场/判决/图鉴等） | 高 | `.sheet` 宽度 `min(640px, 94vw)` 基于 vw，而 overlay 有 20px 内边距，内容盒比 94vw 窄，flex 子项被内容最小宽撑破右缘 |
| 3 | 弹幕层固定 `top:54px`，移动端顶栏变高后被盖 | 中 | 顶栏高度写死 |
| 4 | 竖屏高度分配粗、横屏（如 740×360）仍强制堆叠导致巨长页面 | 中 | 断点只看宽度不看方向 |

主玩法区（对话/问题卡/记录板/追问按钮）窄屏下基本可用，不需重构。

## 2. 修复规格

**顶栏（≤720px）**
- 顶栏改自动高度、纵向两段布局：品牌行居中；数据+按钮行 flex-wrap 居中换行。
- 字号与间距收紧（badge/stat 11px，按钮小号内边距），经验条固定 110px。
- 弹幕层 `top` 由 JS 按 `#topbar` 实际高度设定（`renderTopBar` 时更新），不再写死 54px。

**弹窗（全部 7 个覆盖层通用）**
- `.sheet` 宽度改 `min(640px, 100%)`（以 overlay 内容盒为基准），加 `min-width: 0`。
- `.sheet` 内文本 `overflow-wrap: break-word` 兜底。
- overlay 内边距窄屏收紧到 12px。

**竖屏高度分配（≤860px 竖屏）**
- 客户区 44vh、问题卡区自动撑开，页面可滚动；记录板随流。

**横屏（≤860px 且 landscape）**
- 保持双栏（对话 1.4fr / 问题卡 1fr），body 锁滚动，客户区取消最小高度。

**触控与可读性**
- 按钮最小高度 ≥38px 维持现状；q-card/选项字号 13.5px 维持现状，不额外缩小。

## 3. 验收清单

- [ ] 390×844：顶栏两行整齐无溢出，弹幕不被顶栏遮挡
- [ ] 390×844：开场/判决/图鉴/回访/成就/每日/结算弹窗完整显示，无右缘裁切
- [ ] 740×360 横屏：双栏保持，无超长页面
- [ ] 1440×900 桌面：布局与现状一致（回归）
- [ ] file:// 双击打开行为不变
