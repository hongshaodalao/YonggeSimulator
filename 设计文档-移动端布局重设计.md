# 勇哥模拟器 · 移动端布局重设计 设计文档

> 问题：竖屏手机上问题卡面板排在对话区下方，首屏只露出一条缝；页面滚动与列表内滚动互相竞争，滑动区域有限，体验差。本文档替代《设计文档-移动端适配.md》中的竖屏布局部分（弹窗/顶栏修复继续有效）。

---

## 1. 方案：底部标签页（Tab）布局

竖屏小屏（≤640px）不再让对话区和问题卡纵向堆叠争抢空间，改为**互斥的两个全高标签页** + 底部标签栏切换：

```
┌─────────────────────┐
│ 顶栏（紧凑多行）      │
│ 💡提示（仅首单）      │
│ ┌─────────────────┐ │
│ │  当前标签页       │ │ ← flex:1，占满剩余高度
│ │  连线 OR 问题卡   │ │    内部独立滚动
│ └─────────────────┘ │
│ 📋 记录板（横条）     │ ← 常驻，两个标签页下都可见
│ [💬连线][⚖️判决][❓问题] │ ← 底部标签栏，拇指可达
└─────────────────────┘
```

**规则**
- 标签栏仅 ≤640px 显示；桌面与横屏（>640px）维持现有布局不变。
- 三个标签：💬 连线（客户对话+追问按钮）、⚖️ 判决（中央主按钮，等价原「结束连麦」）、❓ 问题卡（角标实时显示剩余提问名额）。
- 竖屏下隐藏对话区内原有的「结束连麦」按钮（中央判决标签替代它，避免重复）；桌面不变。
- `body` 锁定为 `100dvh` 不再整页滚动，滚动只发生在标签页内部（对话流 / 问题卡列表），消灭双重滚动冲突。
- 新一单开始自动切回「连线」标签；判决/回放是覆盖层不受影响。
- 视口尺寸变化（旋转/缩放）通过 matchMedia 监听：离开小屏布局时自动清除标签状态，恢复双栏。

## 2. 验收清单

- [ ] 390×844：连线、问题卡两个标签页各自占满全高，滚动顺畅，记录板与标签栏常驻可见
- [ ] 问题卡标签角标与剩余名额同步；未提问时判决标签禁用
- [ ] 判决→回放→下一单流程在标签布局下完整可走
- [ ] 740×360 横屏：维持双栏，无标签栏
- [ ] 1440×900 桌面：无回归
